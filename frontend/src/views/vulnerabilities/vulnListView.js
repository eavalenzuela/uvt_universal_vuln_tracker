// PLACEHOLDER

import { el } from "../../ui/dom/el.js";

export async function VulnListView() {
  return el("div", { class: "card" },
    el("h1", { class: "page-title", text: "Vulnerabilities" }),
    el("p", { class: "muted", text: "Placeholder — UI coming next." })
  );
}
